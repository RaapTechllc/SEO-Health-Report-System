{
  "name": "shared-styles",
  "version": "1.0.0",
  "description": "Shared Tailwind CSS styles for dashboard and admin templates",
  "scripts": {
    "build": "npx tailwindcss -i ./input.css -o ./dist/dashboard.css --minify",
    "watch": "npx tailwindcss -i ./input.css -o ./dist/dashboard.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
