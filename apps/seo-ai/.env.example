# Optional. When set, AI-visibility analysis uses live Claude models.
# Without it, the app still runs and returns a deterministic heuristic analysis.
ANTHROPIC_API_KEY=

# Model used for AI-visibility analysis (quality tier).
ANTHROPIC_MODEL=claude-sonnet-4-6
