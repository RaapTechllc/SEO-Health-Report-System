[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --strict-markers
    --strict-config
    -v

markers =
    unit: Unit tests (fast, isolated)
    integration: Integration tests (external dependencies)
    slow: Slow tests (network, long runtime)
    async: Async function tests
    email: Email delivery tests
    scheduler: Scheduler tests
    performance: Performance benchmarks

asyncio_mode = auto

# Uncomment after verifying pytest-cov works:
# addopts =
#     --cov=seo_health_report
#     --cov=seo_technical_audit
#     --cov=ai_visibility_audit
#     --cov=seo_content_authority
#     --cov-report=html
#     --cov-report=term-missing
#     --cov-report=xml
